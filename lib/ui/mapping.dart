import 'package:flutter/material.dart';

class MappingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mapping")),
      body: Center(child: Text("Map goes here")),
    );
  }
}
