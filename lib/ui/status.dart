// status.dart
import 'package:flutter/material.dart';

class StatusPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Robot Status")),
      body: Center(child: Text("Status Info will appear here")),
    );
  }
}